<!DOCTYPE html>
<html>
  <%- include('partials/head', {title: title}) %>
  <body>
    <div class="container-fluid">
      <div class="row">
        <div class="col-sm-2">
          <%- include('partials/sidebar') %>
        </div>
        <div class="col-sm-10">
            <h1><%=title%> : <%=bookinstance.book.title%></h1>
            <p>
                <strong>ID: </strong>
                <span><%= bookinstance.id %></span>
            </p>
            <p>
                <strong>Imprint: </strong>
                <span><%=bookinstance.imprint%></span>
            </p>
            <br>   
            <p>Do you really want to delete this copy?</p>
            <form method="POST" action="">
                <div class='form-group'>
                    <input type="hidden" id="copyid" class="form-control" name="copyid" required="true" value="<%=bookinstance._id%>">
                </div>
                <button class="btn btn-primary" type="submit">Delete</button>
            </form>   
        </div>
      </div>
    </div>
  </body>
</html>